import models.EncodingSchemaE;

import java.io.*;
import java.nio.charset.StandardCharsets;
import java.util.Arrays;

public class Test {

    public static void main(String[] argv) throws IOException {
        System.out.println("hello");
//        final String str = "/kk/";
//        final String[] tokens = str.split("/", 5);
//        System.out.println("token length = " + tokens.length);
//        System.out.println(Arrays.toString(tokens));

//        final String req = "HTTP/1.1 200 OK \r\nAccept-Language: AR\r\nContent-Type: text/plain\r\n\r\n";
//        System.out.println("CRLF bytes " + Arrays.toString("\r\n".getBytes()));
//        InputStream byteStream = new ByteArrayInputStream(req.getBytes(StandardCharsets.UTF_8));
//        InputStreamReader reader = new InputStreamReader(byteStream, StandardCharsets.UTF_8);
//        BufferedReader lineReader = new BufferedReader(reader);
//
//        String line;
//
//        while(true) {
//            line = lineReader.readLine();
//            if ( line == null) {
//                return;
//            }
//
//            System.out.println("line =  " + line);
//            System.out.println("is CRLF line ? " + line.equals(""));
//            System.out.println("line bytes " + Arrays.toString(line.getBytes()));
//        }
    }
}
